<entity-mappings xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence/orm
          http://xmlns.jcp.org/xml/ns/persistence/orm_2_1.xsd" version="2.1">
  <persistence-unit-metadata>
    <persistence-unit-defaults>
    <!-- It is a default db schema for this application. This value has to be synchronized with DDL defined in persistence xml. 
    All db objects will be placed within this schema. 
    No DMLs, queries, in JPQL or Criteria API or initial sql script (defined in persistence.xml) need to be adapted. 
    Apart from ORM.xml whole application remains schema agnostic as it was. -->
      <schema>MARKETING_COMMERCIALAGENT</schema>
    </persistence-unit-defaults>
  </persistence-unit-metadata>

</entity-mappings>